<UserControl x:Class="TgPoolLauncher.Views.JsonGeneratorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
             xmlns:tr="clr-namespace:TgPoolLauncher.Localization.Markup"
             mc:Ignorable="d" d:DesignHeight="700" d:DesignWidth="1080">
    <UserControl.Resources>
        <Style x:Key="SectionCardStyle" TargetType="Border">
            <Setter Property="Background" Value="{StaticResource SurfaceBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="CornerRadius" Value="12" />
            <Setter Property="Padding" Value="20" />
        </Style>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="12" />
            <Setter Property="FontWeight" Value="Bold" />
            <Setter Property="Foreground" Value="{StaticResource TextMutedBrush}" />
        </Style>
        <Style x:Key="ToolbarIconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="34" />
            <Setter Property="Height" Value="34" />
            <Setter Property="Margin" Value="0,0,6,0" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Background" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Foreground" Value="#0C0C0E" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bg" Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="7">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bg" Property="Background" Value="{StaticResource AccentHoverBrush}" />
                                <Setter TargetName="Bg" Property="BorderBrush" Value="{StaticResource AccentHoverBrush}" />
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bg" Property="Opacity" Value="0.42" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style x:Key="PathButtonStyle" TargetType="Button" BasedOn="{StaticResource ToolbarIconButtonStyle}">
            <Setter Property="Width" Value="42" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Margin" Value="8,0,0,0" />
        </Style>
    </UserControl.Resources>

    <Grid Background="{StaticResource AppBackgroundBrush}">
        <ScrollViewer VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto">
        <Grid Margin="16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0.96*" MinWidth="280" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="1.04*" MinWidth="300" />
            </Grid.ColumnDefinitions>

            <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <Grid Margin="0,0,0,14">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{tr:Tr JsonGenerator.SettingsHeader}" Style="{StaticResource SectionHeaderStyle}" VerticalAlignment="Center" />
                        <Button Grid.Column="1" Style="{StaticResource ToolbarIconButtonStyle}" MinWidth="76"
                                Command="{Binding ResetCommand}" ToolTip="{tr:Tr JsonGenerator.ResetTooltip}">
                            <StackPanel Orientation="Horizontal">
                                <iconPacks:PackIconMaterial Kind="Refresh" Width="15" Height="15" Margin="0,0,6,0" />
                                <TextBlock Text="{tr:Tr JsonGenerator.ResetButton}" FontSize="11" FontWeight="SemiBold" />
                            </StackPanel>
                        </Button>
                    </Grid>

                    <Border Style="{StaticResource SectionCardStyle}" Margin="0,0,0,16">
                        <StackPanel>
                            <TextBlock Text="{tr:Tr JsonGenerator.DatabaseLabel}" Style="{StaticResource FieldLabelStyle}" />
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Height="42" Text="{Binding DatabasePath, UpdateSourceTrigger=PropertyChanged}"
                                         ToolTip="{tr:Tr JsonGenerator.DatabaseTooltip}" />
                                <Button Grid.Column="1" Style="{StaticResource PathButtonStyle}"
                                        Command="{Binding BrowseDatabaseCommand}" ToolTip="{tr:Tr JsonGenerator.BrowseDatabaseTooltip}">
                                    <iconPacks:PackIconMaterial Kind="FileDocumentOutline" Width="18" Height="18" />
                                </Button>
                            </Grid>

                            <TextBlock Text="{tr:Tr JsonGenerator.SessionsDirLabel}" Style="{StaticResource FieldLabelStyle}" />
                            <Grid Margin="0,0,0,16">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Height="42" Text="{Binding SessionsDir, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Grid.Column="1" Style="{StaticResource PathButtonStyle}"
                                        Command="{Binding BrowseSessionsDirCommand}" ToolTip="{tr:Tr JsonGenerator.BrowseSessionsDirTooltip}">
                                    <iconPacks:PackIconMaterial Kind="FolderOpenOutline" Width="18" Height="18" />
                                </Button>
                            </Grid>

                            <TextBlock Text="{tr:Tr JsonGenerator.OutputDirLabel}" Style="{StaticResource FieldLabelStyle}" />
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <TextBox Height="42" Text="{Binding OutputDir, UpdateSourceTrigger=PropertyChanged}" />
                                <Button Grid.Column="1" Style="{StaticResource PathButtonStyle}"
                                        Command="{Binding BrowseOutputDirCommand}" ToolTip="{tr:Tr JsonGenerator.BrowseOutputDirTooltip}">
                                    <iconPacks:PackIconMaterial Kind="FolderOpenOutline" Width="18" Height="18" />
                                </Button>
                            </Grid>
                        </StackPanel>
                    </Border>

                    <Button Style="{StaticResource PrimaryButtonStyle}" Height="42" MinWidth="170"
                            HorizontalAlignment="Center" Command="{Binding StartCommand}">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{tr:Tr JsonGenerator.StartButton}" Margin="0,0,8,0" VerticalAlignment="Center" />
                            <iconPacks:PackIconMaterial Kind="PlayCircleOutline" Width="17" Height="17" />
                        </StackPanel>
                    </Button>

                    <TextBlock Text="{tr:Tr JsonGenerator.ImportantInfoHeader}" Style="{StaticResource SectionHeaderStyle}" Margin="0,22,0,12" />
                    <Border Style="{StaticResource SectionCardStyle}" Background="{StaticResource AccentTintBrush}"
                            BorderThickness="0" Margin="0,0,0,4">
                        <StackPanel>
                            <TextBlock Text="{tr:Tr JsonGenerator.WarningParagraph1}"
                                       TextWrapping="Wrap" Margin="0,0,0,12" />
                            <TextBlock Text="{tr:Tr JsonGenerator.WarningParagraph2}"
                                       TextWrapping="Wrap" Margin="0,0,0,12" />
                            <TextBlock Text="{tr:Tr JsonGenerator.WarningParagraph3}"
                                       TextWrapping="Wrap" Foreground="{StaticResource TextMutedBrush}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <Grid Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="Auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Text="{tr:Tr JsonGenerator.ProgramActionsHeader}" Style="{StaticResource SectionHeaderStyle}" VerticalAlignment="Center" />
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Style="{StaticResource ToolbarIconButtonStyle}" Command="{Binding StartCommand}" ToolTip="{tr:Tr JsonGenerator.StartTooltip}">
                            <iconPacks:PackIconMaterial Kind="Play" Width="16" Height="16" />
                        </Button>
                        <Button Style="{StaticResource ToolbarIconButtonStyle}" Command="{Binding StopCommand}" ToolTip="{tr:Tr JsonGenerator.StopTooltip}">
                            <iconPacks:PackIconMaterial Kind="Stop" Width="16" Height="16" />
                        </Button>
                        <Button Style="{StaticResource ToolbarIconButtonStyle}" Margin="0" Command="{Binding ClearCommand}" ToolTip="{tr:Tr JsonGenerator.ClearTooltip}">
                            <iconPacks:PackIconMaterial Kind="DeleteOutline" Width="16" Height="16" />
                        </Button>
                    </StackPanel>
                </Grid>

                <Border Grid.Row="1" Style="{StaticResource SectionCardStyle}" Padding="0" ClipToBounds="True">
                    <DataGrid ItemsSource="{Binding Results}" AutoGenerateColumns="False" IsReadOnly="True"
                              CanUserAddRows="False" BorderThickness="0" HeadersVisibility="Column"
                              GridLinesVisibility="Horizontal">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="{tr:Tr JsonGenerator.ColTime}" Binding="{Binding Time}" Width="84" />
                            <DataGridTextColumn Header="{tr:Tr JsonGenerator.ColAccount}" Binding="{Binding Account}" Width="140" />
                            <DataGridTextColumn Header="{tr:Tr JsonGenerator.ColMessage}" Binding="{Binding Message}" Width="*" />
                        </DataGrid.Columns>
                    </DataGrid>
                </Border>

                <Border Grid.Row="2" Background="{StaticResource SurfaceAltBrush}" CornerRadius="8" Padding="12,9" Margin="0,12,0,0">
                    <StackPanel Orientation="Horizontal">
                        <ProgressBar Width="54" IsIndeterminate="True" Margin="0,0,10,0">
                            <ProgressBar.Style>
                                <Style TargetType="ProgressBar" BasedOn="{StaticResource {x:Type ProgressBar}}">
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                            <Setter Property="Visibility" Value="Visible" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </ProgressBar.Style>
                        </ProgressBar>
                        <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap" Foreground="{StaticResource TextMutedBrush}" />
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
        </ScrollViewer>
    </Grid>
</UserControl>
